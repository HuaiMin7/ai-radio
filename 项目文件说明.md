# 项目文件说明

这个文件夹是 Redio / AI 电台的本地开发项目。Finder 里看到的文件可以分成三类。

## 日常主要看这些

- `web/src/`：前端界面代码，播放器、聊天窗口、播放队列都在这里。
- `server/`：本地后端 API，负责调用大模型、生成推荐、解析音乐、TTS、历史记录。
- `user/`：你的个人资料、音乐口味、作息、结构化歌单。
- `prompts/`：DJ 人设和大模型提示词。
- `public/audio/`：本地测试音频，在线音乐不可播时兜底。
- `scripts/`：本地开发脚本，例如一键启动和健康检查。
- `README.md`：项目运行说明。

## 必须保留，但平时不用点开

- `package.json`：项目脚本和依赖配置。
- `package-lock.json`：依赖版本锁定文件。
- `index.html`：前端入口文件，Vite 需要它。
- `vite.config.ts`：前端开发服务器和 API 代理配置。
- `tsconfig*.json`：TypeScript 配置。
- `AGENTS.md`：给 AI 代码助手看的项目开发规则。
- `.env.example`：环境变量示例，不包含真实密钥。

## 可以重新生成或只是本地运行数据

- `node_modules/`：依赖安装目录，体积最大。删了也能用 `npm install` 重新生成，但删掉后项目暂时跑不起来。
- `dist/`：`npm run build` 生成的前端构建产物，可以删除，需要时会重新生成。
- `cache/`：TTS 语音和后续音乐缓存，可以清理。
- `data/history.json`：本地播放历史和推荐记录。删了会清空历史，但不影响代码。

## 不要提交或公开

- `.env`、`.env.save`：本地密钥和私有配置，不能发给别人。

## 建议

如果只是使用和测试项目，主要关注：

1. `user/playlists.json`
2. `user/taste.md`
3. `web/src/App.tsx`
4. `server/music.ts`
5. `server/brain.ts`
6. `prompts/dj-persona.md`

## 常用命令

```bash
npm run dev:all
```

一键启动前端、后端 API 和网易云本地 API。测试地址是：

```text
http://127.0.0.1:5173/
```

```bash
npm run dev:check
```

检查前端、后端 API 和网易云本地 API 是否都在运行。
